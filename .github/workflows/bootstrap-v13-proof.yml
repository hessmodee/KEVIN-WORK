name: Legacy Bootstrap v1.3 Proof (Archived)

# Historical bootstrap gate retained for provenance only.
# Current production Maintenance is v1.3.13-class typed schema-3 transport and
# inbox/maintenance/manifest.json is a live queue slot. It MUST NOT be interpreted
# as the 2026-08-30 bootstrap manifest or fail unrelated modern maintenance work.
# Active Maintenance gates live with the current runner/candidate workflows.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived gate notice
        run: |
          echo 'ARCHIVED: bootstrap-v13-proof no longer watches the live maintenance manifest.'
          echo 'Reason: the live schema-3 queue evolved beyond the historical bootstrap payload contract.'
          echo 'Do not use this workflow as current Maintenance health evidence.'
