name: Generate Skill Lab v1.0.8 TEMP

on:
  push:
    branches: [bess/skill-lab-v108-converged-20260906]
    paths:
      - 'tools/generate-skill-lab-v1.0.8.py'
      - '.github/workflows/generate-skill-lab-v108-temp.yml'

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: bess/skill-lab-v108-converged-20260906
      - name: Generate candidate
        run: python tools/generate-skill-lab-v1.0.8.py
      - name: Commit deterministic candidate
        run: |
          git config user.name 'Kevin CI'
          git config user.email 'actions@users.noreply.github.com'
          git add control-plane/skill-lab/kevin-skill-lab-v1.0.8.ps1
          if git diff --cached --quiet; then
            echo 'candidate already current'
            exit 0
          fi
          git commit -m 'Generate Skill Lab v1.0.8 converged owner-work runner'
          git push origin HEAD:bess/skill-lab-v108-converged-20260906
