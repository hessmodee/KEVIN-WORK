# LESSON — Hermes / Henry go-max r2 (2026-09-09 19:10)

**Authority:** GREEN-A/C. Observation after the v1.3.55 typed apply. Not PASS.

**Trigger:** Owner asked again to go max: deeply research current Kevin + Hermes/Finn, then execute the next bounded step.

## What changed since the 18:50 lesson

| Signal | 18:46 MT | 19:04–19:07 MT |
|---|---|---|
| Maintenance runner | `EF32D990…` v1.3.53 | **`3E11C429…` v1.3.55** |
| Maintenance status | `EXPIRED_IDLE` v1812 | **`APPLIED_PREAUTHORIZED_PROVEN`** v1355, then `ALREADY_APPLIED_PROVEN` |
| Supervisor | `F17F4B0A…` v1.8.12 | unchanged (required) |
| Benchmark | 30/30 | 30/30 at 19:03:22 |
| Continuation | `BLOCKED_INVOCATION_RUNTIME` 8-vehicle, turn 1 at 09:31 | same. Do not reset. |
| Queue window | v1.3.55 replace_pinned_component | **now legal: `install_invocation_worker_v11`** |

The 18:50 queue was not a skip. HESS-PC applied it. That is the Finn "one employee, prove it, then layer" move.

## Fresh research this turn (do not copy the swarm)

Finn / Henry:

- Reverse prompt: "based on what you know, what is the next best task?" Answer: worker v1.1, then the same 8-vehicle job. Not a new org chart.
- Start with one employee. Still `west-motor-parts-chase-board-pack@1`.
- Mission Control exists so the agent can be proactive. HQ must show v1.3.55 as proven and worker v1.1 as the live rung, not as a queued runner.
- Do not self-upgrade OpenClaw. Typed ladder only.
- Hybrid local muscle. Keep exact-five. Keep Ollama default.

Hermes (Nous, June–Sept 2026):

- Three-layer memory: durable facts, skills, session search. Kevin already has handover + Skill Lab + WorkInstances. Do **not** copy Hermes' ~2200-char `MEMORY.md` cap or fill `HEARTBEAT.md`.
- Compression creates lineage (parent/child sessions), it does not rewrite history. Keep the burned 09:31 turn.
- Tool registration vs exposure. Worker v1.1 still exposes only `west-motor-parts-chase-board-pack@1` until first PASS.
- Observe → reflect → compress → integrate. Compress after proof, not from a fail-closed NativeCommandError.

ClawHavoc still forbids ClawHub/PCClaw: Koi 341/2,857 (11.9%); RankClaw 1,103/14,706 (7.5%); later tallies 1,184. AMOS infostealers, delayed-activation, CVE-2026-32025 ClawJacked. Kevin's 27 PROVEN skills stay local and hashed.

## Next rung (do not skip)

Queue `install_invocation_worker_v11` with **only** the 10 allowed keys. Extra keys (`source_path`, hashes, `target_alias`, `notes`) fail closed. Then wait typed worker proof. Then resume the same WorkInstance.

This lesson is not that proof.
