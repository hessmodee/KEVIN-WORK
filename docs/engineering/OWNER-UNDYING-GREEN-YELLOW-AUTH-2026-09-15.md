# OWNER UNDYING AUTHORIZATION — GREEN + YELLOW — 2026-09-15

**Issued:** 2026-09-15 (America/Denver)  
**Owner:** Matt Hess (HESS-PC)  
**To:** Kevin, Chief of Staff, Grok Build, Grokbot, ChatGPT, Codex, Cursor, Hermes, OpenClaw, and every future replacement agent  
**Status:** STANDING / UNDYING until Matt Hess explicitly revises or revokes

This card **reaffirms and tightens** `docs/engineering/OWNER-UNDYING-GREEN-YELLOW-AUTH-2026-09-04.md` and `inbox/grants/OWNER-STANDING-GRANT-GREEN-YELLOW-v1.md`. It does not create a second handover. Canonical handover remains `AI-HANDOVER.md`.

## The law (plain language)

**GREEN and YELLOW work zones are ALWAYS PERMANENTLY authorized.**

Kevin and any helper building Kevin may **work, build, test, verify, prove, and codify** in GREEN and YELLOW without asking Matt for per-step permission.

**Only the RED work zone requires Matt Hess approval.**

Do not wait. Do not ask-loop. Do not stall GREEN/YELLOW work for a chat confirmation.

## GREEN — standing, no per-task ask

Authorized forever until revoked:

- Read/inspect Kevin repo, HESS workspace, logs, reports
- Diagnostics, benchmarks, isolated tests, Skill Lab
- Self-heal / self-test / self-repair of **already-qualified** Kevin services (Ollama `:11434`, Chat gateway `:18789` via `KevinGatewayKeeper` + `gateway.vbs`, Reader `:19001`, Tick, Supervisor, Skill Lab, GitHub Bridge)
- Proven-skill invocation on existing allowlisted primitives
- Local artifacts under Kevin-approved roots
- Feature branches, PRs, CI, docs, lessons, classifiers, regression tests
- Reversible config in Kevin-owned paths
- Console hygiene / hidden scheduled-task wrappers (no new privileged services)
- Isolated user-space tools and venv/deps under the workspace
- Teach Tick/BootRecovery/Keeper to run repairs Kevin should own next time
- HQ paint that stays evidence-backed (no fake WORKING)

Do not ask Matt to do these.

## YELLOW — standing, receipt-backed, still authorized

Authorized forever until revoked. Each action needs: actor, scope, timestamp, command-or-skill, rollback note, sanitized receipt under `reports/` or `inbox/` (no secrets).

Allowed without a new verbal ask:

- User-level software installs needed for Kevin GREEN work (Python pkgs, gh, git tools)
- Free/safe tools that help the Super-AI north star
- UI Bridge / computer-use inside exact-five plus observe→act→verify tests
- Bounded external research fetches
- Staging typed Engineering Relay verbs that already exist
- Preparing (not sending) communications, posts, carts, bookings
- Applying **already-owner-authorized** production crossings (Desktop exact-five, typed Maintenance/Supervisor packages)

YELLOW still may NOT (those are RED):

- Spend real money / checkout / Amazon / pizza / dealer orders
- Public posts, emails, calls, account password changes as Matt
- Unrestricted admin shell as a product feature
- Internet-exposed RDP/WinRM/new inbound ports
- Night Forge enable
- OpenClaw major version click-upgrade without a written plan + rollback
- PCClaw or unvetted ClawHub/Hermes skills installed into production
- Widen Chat tools past exact-five without a PROVEN operate skill + crossing
- Commit secrets, tokens, private messages
- Weaken benchmark, verifier, authority checks, or receipts
- Label GROKBOT_ACTED / GROK_BUILD_ACTED as KEVIN_ACTED

## RED — Matt Hess approval only

Ask Matt via Chief of Staff only:

- Unrestricted admin shell as a product feature
- Internet-exposed RDP/WinRM/new inbound ports
- Credential vault use beyond existing Kevin paths
- Live financial effects / purchases / live trades
- Public identity actions (posts, emails, calls as Matt)
- Anything that changes the judge so Kevin can pass himself
- Moving the live 8-vehicle job to fixed:main
- Paid APIs / Grokbot credit spend

## Actor honesty (undying)

| Label | Meaning |
|---|---|
| `KEVIN_ACTED` | Tick + Supervisor + Action Era / Chat `main` closed it with **no** `diagnose-*` RequestId and **no** outside-engineer close |
| `MIXED` | Kevin runtime produced artifacts AND an outside engineer published/closed/diagnosed |
| `GROKBOT_ACTED` / `GROK_BUILD_ACTED` | Outside engineer did the work |
| `OWNER` | Matt did it |

Never dress a helper completion as `KEVIN_ACTED`. Missing actor stays `MIXED` (fail-closed).

## Wean rule

Every outside-engineer action must leave Kevin a lesson, test, skill, or Tick/Keeper rule so the same class does not need the outside engineer next time.

End state: Matt talks to **Kevin**, not to Grok Build / Grokbot / ChatGPT, for routine authorized work.

## Does not remove safety invariants

Chat exact-five remains the live Chat allowlist until a PROVEN crossing widens it. No secrets in public artifacts. No inventing Matt's Windows password. Fresh HESS-PC evidence outranks this prose.

## End

Remains in force across sessions, restarts, handoffs, model changes, and replacement agents until Matt Hess explicitly revises or revokes it.
