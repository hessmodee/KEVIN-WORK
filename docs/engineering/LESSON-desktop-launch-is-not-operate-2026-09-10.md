# LESSON — desktop launch is not operate

**Date:** 2026-09-10  
**Authority:** GREEN  
**Family:** `DESKTOP_LAUNCH_NOT_OPERATE`  
**Not PASS. Do not widen Chat tools.**

## Owner symptom

"The only thing I can get him to do is open a calculator. He cannot use the calculator or open or use any other app." Chat then narrates typing, calculating, or writing Notepad files.

## Root cause

Exact-five live Chat tools:

1. `kevin_system_status`
2. `kevin_desktop_find_folder`
3. `kevin_desktop_open_folder`
4. `kevin_desktop_list_folder`
5. `kevin_app_launch`

`kevin_app_launch` starts a fixed local app (Calculator, Notepad, Explorer, Paint). It does not send keys, click, read the display, or save a file. Calculator UIA operate is a **scratch-PROVEN candidate** (`KEVIN_DESKTOP_UI_CALC_V0_OK`) and is **not** Chat-widened. A 16k / tool-free Chat Fast turn that claims success with zero tool results is the existing `LESSON-main-chat-side-effect-claims-require-receipts-2026-09-07` family plus `LESSON-chat-desktop-tools-vs-hallucination-2026-09-04`.

HQ Tools `UNVERIFIED` is a separate paint bug: `reports/main-agent-canary-omen.json` last `OMEN_PROVEN` at 2026-09-08 10:59 MT, freshness 1800s. The five tools still exist. Do not add tools to make the chip green.

## Correct door

The first owner outcome does **not** need desktop UI. `west-motor-parts-chase-board-pack@1` is `create_spreadsheet` + `create_text` through Action Era after the catalog-contract python is live. Widening Chat `tools.allow` before that PASS is forbidden.

After that PASS, typed UI crossings for Calculator operate / Notepad write may be proposed as Skill Lab candidates. Still not Chat-widened by default.

## Kevin next time

- Launch receipt ≠ operate receipt.
- If the requested step needs typing/clicking/saving and exact-five cannot do it, say `NOT_EXECUTED: capability_unavailable`. Do not describe the task as done.
- Do not send the 8-vehicle job to tool-less `fixed:main` or to Notepad.
- Do not install PCClaw / unvetted ClawHub to "use the computer."
