# Kevin vs Henry — what to steal, what to refuse (2026-09-14)

Research against live HESS-PC evidence. Not a second runtime. Not PASS.

## The honest gap

Henry looks autonomous because **the model is the executor**. OpenClaw heartbeat wakes Opus/Qwen, the model reads `HEARTBEAT.md` / `SOUL.md`, and it uses tools. Alex Finn's CoS is a frontier cloud model. Local models are muscles.

Kevin is the opposite on purpose:

| Henry / stock OpenClaw | Kevin on HESS-PC |
|---|---|
| Model plans and acts | Ollama is engine, not planner. `HEARTBEAT.md` stays empty |
| Skills = markdown recipes + ClawHub | Skills = hash-pinned PROVEN composites, Skill Lab |
| Heartbeat every 30m does useful work | Tick + Supervisor + Action Era; model turns are not outcomes |
| CoS talks to the owner | You currently talk to Grok Build / Grokbot because the close-loop is MIXED |
| Install skills from ClawHub | Exact-five desktop. No ClawHub / PCClaw / Hermes plant |

Copying Henry would throw away 33 PROVEN skills and the proof ladder. That is a second agent, not Kevin.

PRs [168](https://github.com/hessmodee/KEVIN-WORK/pull/168), [169](https://github.com/hessmodee/KEVIN-WORK/pull/169), [170](https://github.com/hessmodee/KEVIN-WORK/pull/170) already captured Henry/OpenClaw lessons on 2026-09-09 and were never merged. Do not reinvent that essay. Merge nothing stale onto today's main.

## What *is* useful (steal the pattern)

1. **Closed loop without a human in the hop.** Henry's heartbeat *does work*. Kevin's Tick must select → stage → execute → receipt → COMPLETE on GitHub without a chat paste. That is the only path to `KEVIN_ACTED`.
2. **CoS vs muscles.** Keep Supervisor as selector and Action Era as executor. Do not let Ollama plan.
3. **HEARTBEAT_OK / anti-narcissism.** Silent when nothing is due. Do not manufacture Forge/Skill Lab churn.
4. **Blind-spot atlas** (0xCodio GAP/ATLAS/CHECK). Kevin already has failure families. Today's hole is `ROUTED_TO_PROVEN_SKILL_INVOCATION` + `action_era_ready_count=0` because diagnose still targets a COMPLETE parent. Encode the hole, stop guessing.
5. **Idle builder after silence.** Family-loop refresh of an already-PROVEN key. Not `*-fresh-YYYY-MM-DD` clone PROVE packs.
6. **Local models as insurance** (Alex Finn 13 Sep 2026). Kevin already has Ollama. Downloading extra weights is sovereignty, not a runtime swap.

## What to refuse

- Hermes / ClawHub / PCClaw / stock OpenClaw heartbeat checklists.
- Blind OpenClaw self-upgrade.
- Widening GREEN to pizza, Amazon checkout, voice calls, public posting, Minecraft player. Those stay Delegated Yellow. GREEN already covers the next leap: invoke a PROVEN spreadsheet+text skill on a fresh OPEN WorkInstance.
- Locking a Henry-style "you are my 24/7 teammate, never ask permission, generate an owl" prompt into Kevin. That is puppeteering plus a capability Kevin's invocation allowlist does not have (`create_spreadsheet` + `create_text` only).
- Grok Bot 24/7 courses as the Kevin path. Grokbot is out of credits and is the puppeteer you want to wean off.

## GREEN boundary

Do **not** solve idle by declaring Yellow work Green. Pizza / Amazon / voice / public post / live financial effects remain Delegated Yellow. Prepare grant catalogs only.

The current idle is not an authority shortage. It is a selected-id wiring hole inside GREEN.

## When MIXED stops

`KEVIN_ACTED` requires Tick + Supervisor + Action Era to close the live OPEN WorkInstance with:

- real workbook + companion note
- correlated DONE records
- output hashes
- immutable invocation receipt
- **no** `diagnose-*` RequestId
- **no** chat / Grok Build / Grokbot board-close

Until that receipt exists, actor stays MIXED and `autonomy_credit=false`. Source CI, HQ READY, cycle++, STAGE_OK, and this document are not that receipt.

## Sequence (do not skip)

1. Diagnose v1.3.5 follows live `selected_id` (this change, source only).
2. HESS pulls/runs it. Isolated sim of `invoke-<live-id>` — still not PASS.
3. Worker stages Action Era ready > 0 for that id.
4. Action Era writes workbook + note. Tick closes COMPLETE_DO_NOT_RESELECT on GitHub main.
5. If that close has no diagnose-* and no chat actor → first `KEVIN_ACTED`.
6. Then repeat for other already-PROVEN keys. Then missing-capability loop. Then smallest scoped Yellow grant when the tech is ready.

Grok Build cannot PowerShell HESS-PC. Grokbot is out of credits. Do not paste-run unless floor is stale >20 minutes **and** Tick is dead. Tick/bridge pings are alive; wait the machine, do not puppeteer it.
