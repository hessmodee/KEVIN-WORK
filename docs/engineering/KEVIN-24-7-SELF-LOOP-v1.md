# Kevin 24/7 Self-Loop v1

**Status:** SOURCE PROGRAM — not HESS-PC runtime proof  
**Authority:** GREEN standing + undying GREEN/YELLOW card  
**Does not:** recopy Supervisor v1.8.12, install v1.3.54/v1.3.55 before slot expiry, re-enable Night Forge, widen exact-five, reset history, or claim a queue write as PASS

## Why this exists

Idle-without-reason is a defect. The 8-vehicle invoke is currently fail-closed (`BLOCKED_INVOCATION_RUNTIME`). That is one family. It does not license global idle.

Kevin stays on task 24/7 by cycling four faculties:

| Faculty | Meaning | Proof |
|---|---|---|
| **Self-build** | Construct or repair a capability | Artifact + receipt |
| **Self-test** | Positive, negative, regression | Independent verifier |
| **Self-codify** | Lesson, recipe, skill, failure family | Checked-in source |
| **Self-motivate** | Select next owner-value work and resume interrupted objectives | Selected WorkInstance id |

Permanent loop remains:

`SENSE -> REFLECT -> SELECT -> PLAN -> RESOLVE CAPABILITY -> ACT -> VERIFY -> LEARN -> RESUME`

## Cadence (America/Denver)

| Hours | Slot | Faculty | If P0 invoke is blocked |
|---|---|---|---|
| 00–06 | Night watch | TEST | Keep testing. Night Forge stays Disabled. |
| 06–09 | Dawn brief | MOTIVATE | Rank next independent GREEN objective. |
| 09–12 | Primary act | BUILD | Diagnose worker. Parallel: hygiene + loop packs. |
| 12–15 | Codify | CODIFY | Encode the NativeCommandError family. |
| 15–18 | Yellow incubator | BUILD | Prepare grants/carts. Execute nothing reserved. |
| 18–21 | Comms + HQ | BUILD | Keep HQ honest. Draft, do not send. |
| 21–24 | Learn + resume | MOTIVATE | Resume `owner-west-motor-parts-chase-fresh-8-v1`. |

## GREEN packs this program introduces

These are Skill Lab composites using existing primitives (`create_spreadsheet` + `create_text`). They do **not** require the invocation worker and must **not** wait on it.

1. `kevin-24-7-self-loop-board-pack@1`
2. `kevin-self-test-matrix-pack@1`
3. `kevin-self-codify-log-pack@1`
4. `kevin-yellow-grant-prep-pack@1`

Stage via existing Engineering Relay `stage_composite_skill`. Do not invent a new verb. Do not recreate the 27 proven skills. Do not widen the invocation allowlist until the first fresh PASS.

## Blocked-family switch

When a family is blocked/cooling:

1. Keep the interrupted WorkInstance identity.
2. Select another independent useful GREEN objective.
3. Never delete or reset valid history to create eligible work.
4. After independently proven repair, automatically return to the interrupted objective.

Interrupted objective now: `owner-west-motor-parts-chase-fresh-8-v1`.

## Not PASS

A cadence tick, HQ label, Skill Lab stage, CI pass, model turn, or grant-prep card is not an owner outcome. First PASS remains the 8-vehicle workbook + operating note + DONE records + output hashes + immutable invocation receipt.
