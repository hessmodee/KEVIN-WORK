# Owner Direct Communications + GREEN Mastery Campaign v1

Status: ACTIVE OWNER-DIRECTED CAMPAIGN
Owner directive timestamp: 2026-08-31 00:38 MDT

## Objective

Accelerate Kevin toward a persistent, proactive local Chief of Staff by aggressively exercising, combining, measuring, repairing, and improving every capability already classified GREEN, while preserving typed authority, evidence, rollback, privacy, and bounded failure controls.

Idle-without-reason is a defect. A healthy cycle should execute useful owner-approved work, prove or improve a GREEN skill, diagnose/repair a blocker, encode a reusable lesson, prepare a dependency, or advance another independent mission.

## Standing GREEN execution directive

Kevin should actively use ALL currently proven/authorized GREEN primitives rather than leaving them dormant. For each GREEN capability, maintain current proof level, last successful use, failure rate, latency, owner intervention count, resource pressure, dependencies, and next meaningful composite test.

Repeated manual recovery must become typed self-heal where feasible. Repeated successful primitive sequences should become declarative composite skills through Skill Lab, with deterministic/adversarial testing, replay/idempotency, checkpoint/resume, failure isolation, and measured promotion.

This directive does not convert novel YELLOW/RED authority into GREEN and does not authorize arbitrary shell or arbitrary remote code execution.

## Tonight's owner-visible priority stack

### P0 — Native Matt ↔ Kevin HQ conversation

Build a first-class direct conversation surface in Kevin HQ so Matt can type a message and receive Kevin's reply in the HQ itself.

Required architecture:
- mobile-friendly message composer and transcript panel;
- explicit OWNER -> KEVIN typed message envelope with request ID, timestamp, content hash, status, and provenance;
- typed Kevin reply envelope with correlation/request ID, source model/worker identity, timestamp, completion state, and safe public text;
- no secrets in GitHub Pages, telemetry, browser storage, or public artifacts;
- message text is owner intent/evidence, not arbitrary executable shell;
- any consequential downstream action still passes the existing typed authorization layer;
- idempotent submit/retry behavior so refresh/reconnect does not duplicate work;
- clear QUEUED / RECEIVED / THINKING / REPLIED / FAILED states;
- proof must include real round-trip Matt -> Kevin -> Matt, not mocked UI.

Preferred transport order:
1. existing typed Work Order Intake/control-plane mechanisms where suitable;
2. narrow local relay/endpoint dedicated to owner messages;
3. never embed a GitHub token or local secret into the public HQ client.

### P0 — Telegram recovery

Diagnose the existing Telegram failure using current configuration/state evidence without exposing bot tokens, chat IDs, private message contents, or credentials.

Produce a bounded failure taxonomy first: transport/configuration, bot identity/token enrollment, OpenClaw/channel registration, polling/webhook conflict, network reachability, permissions/chat binding, stale process/task, or message-routing defect.

Repair only through existing typed/local approved mechanisms. If a local secret or Telegram enrollment step is genuinely required, stop at a precise local-only checkpoint. Once repaired, prove private Matt <-> Kevin round trip and do not grant broad external-send authority to arbitrary recipients.

### P0 — Mobile HQ

Treat mobile support as a real responsive UX requirement, not merely a viewport tag.

Required acceptance criteria:
- usable at 360px CSS width without horizontal page scrolling;
- tap targets >= 44px for primary navigation/actions;
- readable cards and telemetry without pinch zoom;
- sticky navigation/composer does not hide content;
- Ops Floor remains truthful and owner-requested visual design stays intact;
- direct chat is comfortable one-handed on phone;
- desktop layout must not regress.

### P1 — GREEN capability mastery

Drive the Computer Capability Matrix by proving useful composites across currently GREEN primitives, emphasizing files, browser observation/navigation candidates that remain within authorized status, Office/document workflows, images/attachments, local app operation, diagnostics, and development/test loops.

No capability is considered mastered merely because code exists. Distinguish DESIGNED, CI-PROVEN, INSTALLED, OMEN-PROVEN, REPEATEDLY-PROVEN, and SELF-RELIANT.

### P1 — Proactivity and persistence

Kevin should maintain a rolling three-steps-ahead queue and opportunistically prepare reversible artifacts/dependencies before owner demand is explicit. When one family cools or blocks, immediately advance another authorized mission.

Measured overnight success should favor completed/proven owner-useful outcomes over counts of commits, generated files, or speculative candidates.

## Communications authority clarification

The owner's current directive explicitly authorizes building and proving private direct communication between Matt and Kevin through Kevin HQ and repairing/proving the previously intended private Telegram channel.

This is a narrow owner<->Kevin communications authorization. It does NOT authorize Kevin to contact third parties, post publicly, represent Matt externally, widen recipient scopes, or treat inbound communication content as executable authority.

## Morning scorecard

Report only measured outcomes:
- direct HQ chat: DESIGN / CI-PROVEN / INSTALLED / OMEN-PROVEN / ROUND-TRIP-PROVEN;
- Telegram: diagnosed root cause, repaired status, round-trip proof status, owner-local blocker if any;
- mobile HQ: viewport(s) validated, major defects fixed, desktop regression status;
- GREEN skills: number exercised, number newly repeatedly-proven, composites promoted, failures/recoveries;
- performance: median/p90 where enough samples exist, retries, owner interventions, recovery time;
- new typed self-heal paths created/proven;
- blockers cooled with exact evidence;
- one genuinely useful surprise accomplishment if one exists.

Never report mock, staged, CI-only, task-presence, matching-hash, or generated-artifact evidence as live user-visible success.

## Guardrails retained

Never create/use arbitrary shell or arbitrary remote code execution; expose credentials/config/private data; weaken safety; silently alter trust anchors; widen recipient/permission scopes; move money; make purchases; live-trade; publish owner-representing external communications; or auto-promote novel YELLOW/RED primitive authority.

Mutable work requires bounded retries, exact preconditions, independent postconditions, semantic success, evidence, idempotency, and rollback. After three materially distinct failures in one family, cool it until evidence changes and work elsewhere.
