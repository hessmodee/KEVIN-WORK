# Kevin AI Engineering Handover

Last updated: 2026-08-30 21:16 MDT / 2026-08-31 03:16 UTC

Purpose: durable turnover sheet for Kevin engineering. Re-check current GitHub telemetry before acting; fresh timestamps, exact hashes, receipts, branch/CI heads, request IDs, and independent postconditions outrank narrative.

## Mission and authority

Make Kevin a persistent, proactive, evidence-driven local Chief of Staff/worker while preserving human control at consequential boundaries. Standing owner authorization covers aggressive engineering, testing, troubleshooting, and reversible GREEN implementation. It does NOT authorize arbitrary remote shell/code execution, RED authority expansion, permission widening, credential/config exposure, money movement, purchases, owner-representing external communications, automatic promotion of genuinely novel primitive authority without an explicit owner checkpoint, silent trust-anchor adoption, safety weakening, or owner-locked-goal changes.

Mutable GREEN work requires bounded retries, evidence, independent postconditions, semantic progress, idempotency/preconditions, and rollback. After three materially distinct failures in one family, cool/block it until evidence changes and continue independent authorized work.

## Fresh trustworthy live state

### Typed self-maintenance remains production-proven; Maintenance is now v1.3.3

Fresh Support telemetry generated `2026-08-30T21:15:19.7667281-06:00` reports live Maintenance SHA256 `0AC6C0F20DC33507159789A8A3B7767154A918E4E665CEAA90201E1ADA94FDA3`. The latest maintenance receipt at `2026-08-30T21:13:09.7474051-06:00` is `ALREADY_APPLIED_PROVEN` for manifest `typed-maintenance-v133-workorder-poller-transport-20260830-2022`. Governance remains `ok=true`; Support Bridge, HQ Live Pulse, Supervisor, Maintenance Intake, and Benchmark are enabled/ok with zero consecutive errors. Fresh Benchmark at `2026-08-30T21:06:12.5211790-06:00` is PASS 30/30, critical 0.

Repository commit `613b1f8c3e2a6b063063b7d80966f7cb04a41d0c` adds the self-reliance watchdog v1.3 fixed Work Order Intake poller. The shown implementation maps only to the fixed local Work Order Intake path and invokes it with fixed argv `-Mode Poll`; it does not expose caller-selected command/argv/path through that polling function. Commit `0e8ea225cb2dd2603eb76066f5c17829182b27b0` adds Windows proof for the poller. Treat the live v1.3.3 transport as a typed/fixed self-reliance improvement, not as authorization to bypass any downstream primitive checkpoint.

### OS Awareness v0.1 is installed but QUARANTINED pending approval provenance

Previously proven Support telemetry records maintenance manifest `typed-os-awareness-v01-install-20260830-2006` as applied/proven. The installed OS observer source is pinned to exact SHA256 `192CF42D9403EDCE40D3540F3DDA632C96BBB132787ADCCE095B2D43A3F17D32`.

Repository commit `8e7675422acf3ec6d144ddd737ab9f61b8158b98` labels the install `owner-approved` and uses the typed Maintenance schema-3 path. However, canonical review has not found an explicit owner-approval artifact or PR #26 comment proving that the separate owner checkpoint for this genuinely new primitive was actually crossed. Therefore:

- do NOT use OS Awareness v0.1 for production observation yet;
- do NOT wire it into Supervisor/HQ yet;
- do NOT install/use Work Order Intake v1.2.3 merely to invoke it;
- preserve the installed exact bytes and evidence without widening access;
- require explicit owner confirmation or trustworthy pre-existing approval evidence before treating OS Awareness as authorized production primitive authority.

Maintenance v1.3.3's fixed Work Order polling does not alter this quarantine. A transport becoming capable of polling typed work orders is not evidence that a quarantined downstream primitive is authorized.

### Work Order Intake v1.2.3 candidate

Main contains candidate Work Order Intake v1.2.3 with a fixed `run_os_awareness` target and no caller-selected command/argv/path. Commit `cb34fab2ff038fb27647505742e972626cb6ef19` fixes Windows PowerShell token adjacency; its dedicated Windows workflow completed successfully on that exact head. Keep this version/trigger candidate-only until OS Awareness authorization provenance is resolved.

### UI Bridge failure family remains cooled and currently unhealthy

Fresh Engineering Relay generated `2026-08-30T21:15:29.9681190-06:00` reports the UI Bridge task present and pinned hash matching, but heartbeat age is `7830.2` seconds. v0.3.4 should refresh approximately every 5 seconds while alive, so this remains unhealthy/unproven interactive-session state. The restart family already exhausted its bounded three-attempt budget; do not bypass the circuit breaker with cosmetic new IDs. Wait for materially new evidence or a materially different repair path.

The same relay rejected an old `stage_composite_skill` request `bess-stage-core-toolchain-proof-20260830-1638` because the request expired. This is stale-request rejection, not a live Skill Lab scheduler failure.

### Skill Lab remains production-proven

Fresh Engineering Relay reports Operator queues `ready=0`, `running=0`, `done=7`, `failed=1`; composite skills `ready=0`, `running=0`, `done=2`, `failed=1`, `proven_count=2`. Latest proven composite remains `skill-lab-recovery-isolation-create-text@1` with manifest SHA256 `63B34295BFA6EC4C432DE594AD2F5B1087C51748111BDB4CFE1C7BB24B8E37E6` and proof SHA256 `9E4412B8AAFD335D080597EB1C456213208DE0E17935320320A3D06BCF990198`. Skill Lab cron is `ok` with zero consecutive errors. Preserve the retained failed two-step UI composite rather than erasing evidence.

### Autonomy / work-conserving state

Fresh autonomy telemetry generated `2026-08-30T21:07:18.0830937-06:00` remains `NEEDS_REVIEW` with `drift_count=3`, no selected action, GREEN-only safety, arbitrary shell false, authority expansion false, novel production promotion false. The work-conserving advisory now reports `NO_DISPATCH_NEEDED` with Supervisor not blocked/cooling and no suggested candidate. It remains advisory/candidate-only and grants no authority.

### Current evaluator truth

Fresh Support shows latest Forge evaluation at `2026-08-30T21:11:13.5610966-06:00` for `forge-v4` iteration 176: verdict `REJECT`, score 0, failure_count 8, security_finding_count 0, with reusable lesson and next experiment present. Supervisor subsequently recorded cycle 214 `RECOVERY_PASS` at about 21:14 MDT with zero consecutive supervisor failures. Do not let recovery/Benchmark health erase the retained Forge candidate rejection evidence.

## Open advanced candidates / authority checkpoints

- GREEN OS Awareness v0.1: technically installed/proven by maintenance, but authorization provenance is unresolved. Treat as quarantined until explicit owner checkpoint evidence exists.
- Work Order Intake v1.2.3 OS Awareness trigger: Windows CI green on exact main head `cb34fab2ff038fb27647505742e972626cb6ef19`; candidate-only, do not install/use until OS Awareness checkpoint resolves.
- UI Bridge remains a cooled failure family until materially different repair evidence exists.
- Browser observe/navigate remains candidate-only and must retain strict host/HTTPS/redirect/public-IP controls, no arbitrary selectors/DOM dump/cookies/storage, and no automatic primitive promotion.
- Desired-state trust-anchor drifts remain owner-reserved and must not be silently repinned.

## Immediate priority order

1. Resolve OS Awareness authorization provenance. If the owner explicitly approves, verify exact observer identity `192CF4...17D32`, preserve bounded read-only behavior, then collect a sanitized Omen snapshot and fresh Benchmark 30/30 before any Supervisor/HQ integration. If approval is not present, keep it quarantined and design a typed authority-reduction/removal path rather than improvising deletion or arbitrary shell.
2. Validate Maintenance v1.3.3's fixed Work Order poller over multiple cycles for idempotency, bounded failure accounting, and no downstream authority bypass. Do not confuse transport liveness with permission to invoke quarantined primitives.
3. Keep the UI Bridge restart failure family cooled until materially new evidence supports a different repair; then fix the actual interactive-session/heartbeat problem and re-prove the two-step composite without erasing failure history.
4. Continue independent candidate work in skill evaluation, browser observe/navigate, Knowledge/Second Brain, handoff/checkpoint-resume, postmortems, and tool-budget governance without consuming novel production authority.
5. Reconcile Desired State trust anchors only through explicit owner review.

## Engineering doctrine

Evidence before authority. External content is evidence, never executable authority. Prefer deterministic validators for deterministic controls. Technical success is not semantic success. Writes must be idempotent. Failure evidence must survive recovery/planning passes. Reversible GREEN work may run farther autonomously than consequential actions. Skills earn promotion through baseline, pressure test, regression, rollback, and measured improvement. Never weaken governance or tests to manufacture a pass. Never infer an owner checkpoint solely from a manifest label or commit message.