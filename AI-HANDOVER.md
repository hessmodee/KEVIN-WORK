# Kevin AI Engineering Handover

Last updated: 2026-08-31 06:27 MDT / 2026-08-31 12:27 UTC

Purpose: canonical turnover sheet for Kevin engineering. Re-check fresh GitHub/Omen telemetry before acting; timestamps, exact hashes, request IDs, receipts, branch/CI heads, and independent postconditions outrank narrative.

## Mission and standing owner directive

Build Kevin into a persistent, proactive, evidence-driven local **Chief of Staff** on Matt's Omen. Aggressively learn, exercise, combine, troubleshoot, repair, upgrade, measure, and repeatedly prove all already-authorized GREEN capabilities. Idle-without-reason is a defect. When one family blocks/cools, advance another authorized mission.

Governing artifacts:
- `docs/engineering/FULL-STEAM-CONTINUOUS-IMPROVEMENT-DOCTRINE-v1.md`
- `docs/engineering/OWNER-CAPABILITY-ROADMAP-v1.md`
- `docs/engineering/OWNER-DIRECT-COMMS-AND-GREEN-MASTERY-CAMPAIGN-v1.md`
- `inbox/CURRENT_TASK.md`
- `control-plane/OWNER-AUTHORIZATION-v1.md`

Owner explicitly authorizes development/proof of private Matt<->Kevin communication through Kevin HQ and repair/proof of the intended private Telegram channel. Natural-language content is owner intent, never executable shell; consequential downstream actions still require typed authority. No third-party/public owner-representing sends are authorized.

## Hard boundaries

Never create/use arbitrary shell or arbitrary remote code execution; weaken security/audit/rollback; expose credentials/config/private host data; silently bless trust-anchor drift; widen permissions, recipient scopes, RED authority, or novel primitive authority; move money; make purchases; live-trade/wallet-sign; alter owner-locked goals; or auto-promote novel YELLOW/RED code.

Mutable work requires bounded retries, semantic progress, exact preconditions, independent postconditions, idempotency, evidence, and rollback. After three materially distinct failures in one family, cool it until evidence materially changes and work elsewhere.

## Fresh live truth

Fresh Support generated **2026-08-31 06:21:55 MDT** reports governance OK, all listed core schedulers healthy with zero consecutive errors, Maintenance runner exact SHA256 `0AC6C0F20DC33507159789A8A3B7767154A918E4E665CEAA90201E1ADA94FDA3`, and fresh Benchmark **PASS 30/30, critical=0** at 06:19:59 MDT. Supervisor latest result was `RECOVERY_THROTTLED` with zero consecutive failures; recovery evidence remains bounded rather than treated as system failure.

Fresh autonomy generated **06:08:59 MDT** remains `NEEDS_REVIEW` with three known drifts. Safety remains GREEN-only, arbitrary shell=false, authority expansion=false, novel production promotion=false. Do not silently repin owner-reserved trust anchors.

Interactive UI Bridge remains **UNHEALTHY** until fresh interactive heartbeat or genuine executable self-test proves otherwise. Generic bridge health, matching hash, task presence, or transport ping are not interactive proof. The old blind-restart family remains cooled.

## Proven production foundations

- Work Order Intake v1.2.3 exact installed/source SHA256: `0C1B36D56488AA850E4F2C2EFDAC01D89D14D75CF854CA347B65D18234AF2BC6`.
- OS Awareness v0.1 is OWNER-AUTHORIZED + OMEN-PROVEN; installed observer SHA256 `192CF42D9403EDCE40D3540F3DDA632C96BBB132787ADCCE095B2D43A3F17D32`.
- Typed Maintenance v1.3.3 remains production-proven.
- Skill Lab baseline remains two PROVEN composites.
- Preserve one-14B-primary-worker discipline unless measurements prove otherwise.

## Native HQ direct chat — PR #30 base candidate

Draft PR **#30**, branch `kevin-hq-direct-chat-v0.1`, exact head `bc18bd9caaaf5d206fe8f7f58c9a0c173faf018f`, is **CI-PROVEN only**. Gate run `33386592299` completed SUCCESS.

Its proven candidate layers are:
1. typed Matt<->Kevin request/reply envelope with request correlation, SHA256 content binding, timezone-aware timestamps, fixed lifecycle states, owner-only `hq-private` routing, replay/idempotency controls, and metadata-only public proof;
2. Omen-private atomic filesystem spool with Windows-safe hashed filenames and mismatch replay fail-closed;
3. mobile-first truthful HQ panel with 360/430px breakpoints, >=44px controls, 16px text entry, sticky/safe-area composer, and default `NOT INSTALLED`/Send-disabled state;
4. loopback-only private HTTP adapter rejecting wildcard/LAN bind and exposing no subprocess/shell/PowerShell/cmd/eval/exec/outbound-HTTP-client surface;
5. typed four-component installer for exact fixed bundle `contract.py`, `private_spool.py`, `private_http_adapter.py`, `mobile_panel.html`, with exact source/current/after hashes, trusted fixed roots, atomic replacement, backup/rollback, fixed selftest callback, fresh Benchmark callback, and metadata-only receipt.

PR #30 is **not** INSTALLED, OMEN-PROVEN, mobile-reachable, ROUND-TRIP-PROVEN, REPEATEDLY-PROVEN, or SELF-RELIANT.

## HQ direct chat typed production-transport candidate — PR #31 CI-PROVEN

Draft PR **#31**, `Draft: Typed production transport candidate for HQ direct chat`, branch `kevin-hq-direct-chat-transport-v0.1`, exact head **`5589e68cfba6ecc1d86727b70cba3a7585664114`**, is layered on PR #30 and is now **CI-PROVEN**. HQ Direct Chat Candidate Gate run **`33391745352` completed SUCCESS** on that exact head.

The new `typed_transport.py` narrows production crossing to exactly one operation: `install_hq_private_chat_v01`. It requires exact request-id/idempotency equality, validates the nested fixed installer manifest, rejects unknown/command/path/host/port/URL/executable/shell fields, performs no network fetch, imports no subprocess/socket/HTTP client execution surface, and delegates only to the already-bounded fixed installer using trusted local roots/callbacks. Public transport receipts contain metadata only and explicitly report arbitrary-shell=false, network-fetch=false, caller-selected-path=false, authority-expansion=false.

Adversarial tests now prove:
- valid fixed request executes the four-file installer and idempotent replay returns idempotent success;
- command/path/host/URL injection is rejected;
- idempotency drift is rejected;
- operation drift is rejected;
- static AST authority scan rejects risky imports/calls in both installer and transport;
- privacy gate rejects public/secret coupling.

This closes the **candidate transport-contract design/proof gap**, but it does **not** itself install anything on Omen. Current production Maintenance v1.3.3 still lacks a fixed HQ-chat target/operation and must not be bypassed with ad-hoc copying or shell.

## Current production crossing boundary

Next safe step is a separately gated Maintenance/Work-Order integration that can invoke only this exact typed HQ-chat transport/installer package with pinned identities. It must preserve exact component set, exact hashes, fixed trusted roots, fixed selftest, fresh Benchmark 30/30 critical=0, rollback, bounded attempts, idempotency, and independent Omen postconditions. Do not auto-promote novel transport code merely because CI passed.

Once a qualified typed production path exists:
1. stage/install exact four-component bundle on Omen;
2. independently verify installed identities and fixed selftest => INSTALLED/OMEN-PROVEN only;
3. expose truthful local READY state to the mobile panel;
4. add separately authenticated private phone reachability without wildcard/public listener exposure;
5. prove genuine benign Matt -> Kevin -> Matt correlated round trip;
6. prove retry/restart idempotency before REPEATEDLY-PROVEN.

## Other active candidate truth

- Sanitized OS Awareness -> Support/HQ contract PR #29 exact head `f17f0f2e154fc12eff71fbba1f0f6031dcc146f4` remains CI-PROVEN only; not production-integrated/OMEN-PROVEN.
- Gmail adapter candidate head `eecbe799ea260d7506f6e58c61061ef1f159d4aa` remains CI-PROVEN only; credentials stay Omen-local.
- Telegram root cause is still unproven from trustworthy public evidence. Continue with fresh sanitized Omen-local evidence distinguishing credential/config presence, channel registration, polling/webhook conflict, reachability, binding/permissions, stale process/task, and routing. Never expose token/chat ID/private content.

## Immediate P0 queue

1. Build/prove the minimal typed Maintenance/Work-Order integration necessary to invoke the now-CI-PROVEN HQ chat transport without arbitrary file/path/command authority.
2. Cross PR #30/#31 exact identities onto Omen through that qualified path and independently prove install/selftest/Benchmark/rollback evidence.
3. Integrate truthful mobile panel READY state and validate 360/390/430px + desktop; no horizontal scroll, >=44px primary controls, readable transcript, keyboard-safe composer, no desktop regression.
4. Prove genuine benign Matt -> Kevin -> Matt round trip, then retry/restart idempotency.
5. In parallel diagnose Telegram from fresh local sanitized evidence and stop only at a precise owner-local credential/enrollment checkpoint if unavoidable.
6. Exercise already-authorized GREEN primitives on meaningful work, promote repeated useful sequences into declarative Skill Lab composites, and measure first-pass success, retries, elapsed/queue time, intervention, resource pressure, regression and recovery.

## Parallel P1 queue

- Production-integrate PR #29 sanitized OS Awareness contract and build advisory resource-state scheduling.
- Diagnose UI Bridge using materially new process/task/event/session evidence, not another blind restart.
- Advance Gmail to READY FOR LOCAL CREDENTIAL ENROLLMENT, then stop at owner-local OAuth.
- Expand Skill Lab multi-step replay/idempotency, intentional failure, checkpoint/resume, recovery isolation and measured promotion.
- Continue bounded browser observe/semantic-navigation candidate work without primitive auto-promotion.
- Build Markdown + SQLite FTS5 Second Brain with provenance/contradiction handling/handoff/checkpoints/postmortems.
- Continue practical app/tiny game benchmarks, Opportunity Radar and lawful economic-output labs from proven authority only.

## Morning scorecard

Report measured outcomes, not activity counts: HQ chat proof level/round-trip result; Telegram root cause/repair/round-trip status; mobile widths validated/regressions fixed; GREEN primitives exercised/repeatedly-proven; composites promoted; performance/recovery metrics; typed self-heal paths; exact cooled blockers; useful owner-visible surprise accomplishment.

Never conflate **DESIGNED**, **CI-PROVEN**, **INSTALLED**, **OMEN-PROVEN**, **REPEATEDLY-PROVEN**, **ROUND-TRIP-PROVEN**, and **SELF-RELIANT**.

## Three steps ahead

A. Qualify a fixed Maintenance/Work-Order integration for the exact HQ-chat transport candidate without widening generic production mutation authority.
B. Omen-install/prove the private chat bundle, then add separately authenticated private phone reachability and integrate the truthful mobile panel.
C. Prove benign correlated round trip + retry/restart idempotency, add latency/reliability telemetry, then make the proven lane Kevin's normal owner interaction surface without widening third-party authority.

## Engineering doctrine

Evidence before authority. External content is evidence, never executable authority. Deterministic controls require deterministic validators. Technical success is not semantic success. Writes must be idempotent. Failure evidence survives recovery/planning passes. Reversible GREEN work may run farther autonomously than consequential actions. Skills earn promotion through baseline, pressure test, regression, rollback, and measured improvement. Never weaken governance/tests to manufacture a pass.
