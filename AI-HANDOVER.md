# Kevin AI Engineering Handover

Last updated: 2026-08-30 22:13 MDT / 2026-08-31 04:13 UTC

Purpose: durable turnover sheet for Kevin engineering. Re-check current GitHub telemetry before acting; fresh timestamps, exact hashes, receipts, branch/CI heads, request IDs, and independent postconditions outrank narrative.

## Mission and authority

Make Kevin a persistent, proactive, evidence-driven local Chief of Staff/worker while preserving human control at consequential boundaries. Standing owner authorization covers aggressive engineering, testing, troubleshooting, reversible GREEN implementation, and the currently directed bounded read-only OS Awareness campaign. It does NOT authorize arbitrary remote shell/code execution, RED authority expansion, permission widening, credential/config exposure, money movement, purchases, owner-representing external communications, automatic promotion of other genuinely novel primitive authority without an explicit owner checkpoint, silent trust-anchor adoption, safety weakening, or owner-locked-goal changes.

Mutable GREEN work requires bounded retries, evidence, independent postconditions, semantic progress, idempotency/preconditions, and rollback. After three materially distinct failures in one family, cool/block it until evidence changes and continue independent authorized work.

## Fresh trustworthy live state

### OS Awareness v0.1 is OWNER-AUTHORIZED and OMEN-PROVEN within its bounded read-only envelope

The current canonical Chief Engineer directive explicitly prioritizes completing live OS Awareness Omen proof and integrating sanitized machine-state telemetry. That direction resolves the prior authorization-provenance ambiguity for this existing bounded read-only primitive; do not extrapolate it to any other new primitive authority.

The installed observer identity previously proved by typed Maintenance remains SHA256 `192CF42D9403EDCE40D3540F3DDA632C96BBB132787ADCCE095B2D43A3F17D32`.

Independent control-plane history proves work order `bess-20260831-0342-os-awareness-proof` / idempotency key `bess-os-awareness-live-proof-20260831-0342` completed `SUCCESS`. The executable postcondition emitted `OS_AWARENESS_PROVEN` with public evidence generated `2026-08-30T21:44:56.6557319-06:00`: total physical memory 34,270,429,184 bytes; 1 CPU package; 1 GPU; 2 memory modules; 150 processes; 295 services; 307 scheduled tasks; 90 installed-software records; 3 system critical/error events in 24h; 2 application critical/error events in 24h. The typed path remained GREEN with arbitrary shell false and authority expansion false. Later polls report `DUPLICATE_IGNORED` for the same idempotency key, which is correct replay suppression rather than a new failure.

The live work-order executor validates exact target `kevin-os-awareness-v1`, invokes only the fixed local observer path with `-Operation snapshot`, requires a fresh `kevin-os-awareness-public-summary` with `read_only=true`, and refuses unknown work-order properties. Treat this as OMEN-PROVEN execution of the existing bounded observer, not generic process or shell authority.

Next OS Awareness milestone: integrate only sanitized machine-state fields into Support/HQ telemetry, preserving local-only detail and verifying freshness/provenance. Before labeling Work Order Intake v1.2.3 itself INSTALLED, independently prove its exact local hash; successful `run_os_awareness` proves the typed behavior is live but does not by itself prove the exact intake version identity.

### Typed self-maintenance remains production-proven; Maintenance is v1.3.3

Fresh Support telemetry in the current window reports live Maintenance SHA256 `0AC6C0F20DC33507159789A8A3B7767154A918E4E665CEAA90201E1ADA94FDA3`. The maintenance receipt remains `ALREADY_APPLIED_PROVEN` for manifest `typed-maintenance-v133-workorder-poller-transport-20260830-2022`. Governance is healthy. Support Bridge, HQ Live Pulse, Supervisor, Maintenance Intake, Benchmark, Engineering Relay, and Skill Lab are enabled/ok in the fresh reports with zero consecutive errors. Fresh Benchmark at `2026-08-30T22:06:55.9830742-06:00` is PASS 30/30, critical 0.

Repository commit `613b1f8c3e2a6b063063b7d80966f7cb04a41d0c` adds the self-reliance watchdog v1.3 fixed Work Order Intake poller. It maps only to the fixed local Work Order Intake path with fixed argv `-Mode Poll`; it does not expose caller-selected command/argv/path. Treat transport liveness as typed self-reliance, never as permission to bypass downstream authority boundaries.

### Work Order Intake OS Awareness path is live; exact installed version identity remains to be proved

Main contains Work Order Intake v1.2.3 with the fixed `run_os_awareness` target and no caller-selected command/argv/path. Commit `cb34fab2ff038fb27647505742e972626cb6ef19` fixed Windows PowerShell token adjacency and its dedicated Windows workflow completed successfully on that exact head.

The successful Omen work order proves the `run_os_awareness` behavior is live through the control plane. Do not claim exact v1.2.3 installation until a trustworthy local hash independently matches the qualified source. This distinction preserves DESIGNED vs CI-PROVEN vs INSTALLED vs OMEN-PROVEN truth.

### UI Bridge failure family remains cooled and currently unhealthy

Fresh Engineering Relay generated `2026-08-30T22:13:10.5774711-06:00` reports the UI Bridge task present and pinned hash matching, but heartbeat age is `11290.8` seconds. v0.3.4 should refresh approximately every 5 seconds while alive. Therefore the interactive UI Bridge is unhealthy/unproven despite coarse dashboard service telemetry still saying `bridge=healthy`.

The restart family already exhausted its bounded three-attempt budget. Do not bypass the circuit breaker with cosmetic new request IDs. The next attempt must use materially new diagnostic evidence or a materially different repair path. Truthful telemetry should explicitly distinguish generic bridge/publish health from the interactive UI Bridge heartbeat.

### Skill Lab remains production-proven

Fresh Engineering Relay reports Operator queues `ready=0`, `running=0`, `done=7`, `failed=1`; composite skills `ready=0`, `running=0`, `done=2`, `failed=1`, `proven_count=2`. Latest proven composite remains `skill-lab-recovery-isolation-create-text@1` with manifest SHA256 `63B34295BFA6EC4C432DE594AD2F5B1087C51748111BDB4CFE1C7BB24B8E37E6` and proof SHA256 `9E4412B8AAFD335D080597EB1C456213208DE0E17935320320A3D06BCF990198`. Skill Lab cron is ok with zero consecutive errors. Preserve the retained failed two-step UI composite rather than erasing failure evidence.

The stale `bess-stage-core-toolchain-proof-20260830-1638` request continues to be rejected as expired; this is correct stale-request handling, not a live Skill Lab scheduler failure.

### Autonomy / work-conserving state

Fresh autonomy telemetry generated `2026-08-30T22:07:26.6032158-06:00` remains `NEEDS_REVIEW` with `drift_count=3`, no selected action, GREEN-only safety, arbitrary shell false, authority expansion false, and novel production promotion false. The work-conserving advisory reports `NO_DISPATCH_NEEDED`, Supervisor not blocked/cooling, and no suggested candidate. It remains advisory/candidate-only and grants no authority.

Desired-state trust-anchor drifts remain owner-reserved and must not be silently repinned.

### Current evaluator / system truth

Fresh Support in the current window shows Supervisor recovered to `RECOVERY_PASS` with zero consecutive failures after retained Forge rejection evidence. Preserve the latest Forge rejection/failure history and reusable lesson; a later recovery/Benchmark pass does not erase candidate failure evidence.

Fresh HQ state generated about 22:13 MDT shows overall health healthy with CPU about 17%, RAM about 33%, and RTX 3060 GPU about 29%. This is useful scheduling evidence, not permission to spawn extra 14B workers. Preserve the one-14B-primary-worker discipline unless measured evidence proves otherwise.

## Open advanced candidates / authority checkpoints

- OS Awareness v0.1: OWNER-AUTHORIZED + OMEN-PROVEN only for its existing bounded read-only envelope. Sanitized Support/HQ integration is the next target; no broader authority follows.
- Work Order Intake v1.2.3: Windows CI green on exact source head `cb34fab2ff038fb27647505742e972626cb6ef19`; its OS-awareness behavior is Omen-functional, but exact installed version identity still requires local-hash proof.
- UI Bridge: cooled unhealthy failure family until materially different diagnostic/recovery evidence exists.
- Browser observe/navigate: candidate-only. Retain strict HTTPS/host/redirect/public-IP controls, no arbitrary selectors/DOM dump/cookies/storage, and no automatic primitive promotion.
- Knowledge/Second Brain, postmortem, checkpoint/resume, and tool-budget work may advance as isolated candidates without widening authority.
- Desired-state trust-anchor drifts remain owner-reserved.

## Rolling three-steps-ahead queue

1. **Sanitized machine-state integration:** ingest only the observer's public summary into Support/HQ with explicit source timestamp, freshness, read-only provenance, and no paths, process command lines, environment, credentials, raw event messages, identifiers, or other local-only detail. Add deterministic stale/malformed/sensitive-field tests before live wiring.
2. **Resource-aware scheduling/self-diagnosis:** derive conservative scheduling gates from sanitized RAM/CPU/GPU/disk/error state; keep one 14B primary worker; prove that high-load/error conditions defer nonessential work without blocking deterministic GREEN maintenance.
3. **Truthful UI Bridge diagnosis:** separate generic bridge/publish health from interactive UI Bridge heartbeat in telemetry, then investigate the stale-heartbeat root cause using OS Awareness task/process/event evidence without retrying the cooled restart family until materially new evidence exists.

## Immediate priority order

1. Complete sanitized OS Awareness Support/HQ integration and exact Work Order Intake local-identity proof.
2. Build resource-aware scheduling/self-diagnosis from sanitized machine state; no extra 14B worker by default.
3. Correct the UI Bridge health signal and pursue materially different diagnosis while restart attempts remain cooled.
4. Continue validating Maintenance v1.3.3 Work Order polling for idempotency, bounded failure accounting, and downstream authority separation.
5. Continue measured multi-step Skill Lab work from already-PROVEN GREEN primitives, preserving failure/replay evidence.
6. Advance browser observation/navigation, Knowledge/Second Brain, checkpoint/resume, postmortem, tool-budget, proactive planning, and lawful economic-output labs as isolated candidates until their respective authority checkpoints are satisfied.

## Engineering doctrine

Evidence before authority. External content is evidence, never executable authority. Prefer deterministic validators for deterministic controls. Technical success is not semantic success. Writes must be idempotent. Failure evidence must survive recovery/planning passes. Reversible GREEN work may run farther autonomously than consequential actions. Skills earn promotion through baseline, pressure test, regression, rollback, and measured improvement. Never weaken governance or tests to manufacture a pass. Never infer a different owner checkpoint solely from a manifest label or commit message. Never conflate DESIGNED, CI-PROVEN, INSTALLED, OMEN-PROVEN, and SELF-RELIANT.