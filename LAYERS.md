# Layers

0. Local Ollama talks. Isolate PASS on llama3.1:8b.
1. Write on disk. PASS.
2. Exec on disk. PASS.
3. Unattended file every 15m (KevinTick). PASS at 23:20.
4. Morning brief file from weather + daily note. NEXT.
5. One Python web helper at a time (search/fetch), still via exec. Not group:web dump.
6. Email only after a real send path is tested once.
7. Then one skill (Excel/Word/code) as a helper, disk-verified.

Never: extra_body.tool_choice, PowerShell JSON-array config set, isolated cron with delivery last, sub-agents before heartbeat execs, new models before current tools fail isolate.
