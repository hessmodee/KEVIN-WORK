# Kevin Core

Read-only OpenClaw tool plugin for Kevin Reader.

One tool: `kevin_system_status`. Optional until explicitly allowlisted.

It runs a fixed Python helper. It never accepts a command, path, or shell string.

```
npm install
npm run plugin:validate
npm test
```

Do not enable this on Chat v2. Reader only.
